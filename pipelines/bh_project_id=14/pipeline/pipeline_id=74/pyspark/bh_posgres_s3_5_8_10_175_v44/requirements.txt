# Generated by bh-bhengine-pyspark.
# Pip packages for the driver process. JDBC/S3/Kafka I/O also needs JVM jars
# configured in main.py via spark.jars.packages (see below).

pyspark>=3.5,<4
pyyaml>=6.0
boto3>=1.34

# Detected connection types: PostgreSQL, S3
#
# JVM jars (also set on SparkSession in main.py):
#   org.postgresql:postgresql:42.7.12
#   org.apache.hadoop:hadoop-aws:3.4.3
#   software.amazon.awssdk:bundle:2.35.4
#
# Notes:
#   - Base: PySpark app + YAML connection loader.
#   - boto3: used by emr.py fetch_secret() for AWS Secrets Manager lookups.
#   - main.py installs this file automatically when packages are missing.
#   - PostgreSQL: Spark JDBC uses org.postgresql.Driver — jar via spark.jars.packages.
#   - psycopg2 is NOT required for Spark JDBC I/O (only optional Python pre/post SQL).
#   - S3: s3a:// paths need hadoop-aws + AWS SDK on the classpath before session start.
#   - boto3 (in base) covers Secrets Manager only; emitted s3a I/O goes through the jars.
