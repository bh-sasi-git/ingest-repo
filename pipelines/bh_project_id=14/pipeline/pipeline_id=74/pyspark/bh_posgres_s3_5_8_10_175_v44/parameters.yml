# Pipeline parameter defaults (from StreamSets constants / BH parameters).
# Override at submit: --param KEY=VAL, BH_PIPELINE_PARAMS JSON, or a
# second parameters overlay shipped with the job.

B: "s3a://cleverbits1/bh-test-postgres"
F: JSON
